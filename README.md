# Forge (working title)

Structured strength programming + training-load-adaptive nutrition. See `../prd.html`, `../engineering-backlog.html`, and `../Wireframes/` for product context.

## Structure

- `Sources/ForgeCore` — pure Swift business logic (Load Score, TDEE, macro/calorie engine). No UIKit/SwiftUI dependency, builds and tests with just the Swift toolchain — no Xcode required.
- `Tests/ForgeCoreTests` — Swift Testing (`import Testing`) coverage for the engine above.
- `App/` — the SwiftUI iOS app. Requires Xcode to build.
- `project.yml` — [XcodeGen](https://github.com/yonaskolb/XcodeGen) spec. The `.xcodeproj` is generated, not committed — regenerate after adding files.
- `FoodProxy/` — a small Node/Express server that proxies FatSecret API calls (see its README for why: FatSecret requires a fixed, allowlisted IP and server-held credentials, which a mobile app can't provide directly). Deployed separately (Render), not part of the Xcode project.

## Getting started

Xcode is installed on this machine. XcodeGen isn't available via Homebrew here (Homebrew itself isn't installed), so it was built from source once — if you need to regenerate the project after adding files and don't have `xcodegen` on PATH, either `brew install xcodegen` or build it yourself:

```
git clone --depth 1 https://github.com/yonaskolb/XcodeGen.git /tmp/XcodeGen
cd /tmp/XcodeGen && swift build -c release --product xcodegen
```

Then from this folder: `xcodegen generate`, open `Forge.xcodeproj`, select a simulator, and run.

To run just the engine tests: `swift test`.

## Status (2026-07-20)

**Done and verified — both compiled and run in Simulator (iPhone 17), not just compile-checked:**
- `ForgeCore` — Load Score, TDEE/macro calculation, RED-S floor guardrail, carb-band selection, weekly weight-trend recalibration (FRG-301), progressive-overload suggestions (FRG-113), exercise library (`free-exercise-db`, 873 exercises), food database layer (USDA FDC + Open Food Facts + FatSecret, see below). 33 tests passing (`swift test`), stable across repeated runs.
- Onboarding (4 steps), Today, Target Explanation sheet, Train (per-exercise sets with RPE picker), Eat (diary + **live food search** against real USDA/Open Food Facts data, barcode scan entry point, recent/frequent foods), Progress, You. Both light and dark mode confirmed.
- **Food database (FRG-120–124, P0) + barcode scanning (FRG-125, P1) + recent/frequent foods (FRG-302, P1)**: `FoodSearchService` queries USDA FDC, Open Food Facts, and FatSecret concurrently and merges results — all three confirmed live, not mocked. FatSecret is fully working end-to-end: deployed proxy (`FoodProxy/` on Render) → Premier-Free IP allowlisting (CIDR ranges, unlocked by FatSecret's startup program approval) → real search results flowing into the app (verified: "chicken breast" returns 1,545 real FatSecret results including branded items). One real bug found and fixed getting here — `FATSECRET_CLIENT_ID` had a leading space in Render's env var, invisible in the dashboard, silently breaking the OAuth Basic-Auth header; found via a temporary diagnostic endpoint, fixed, and the proxy now trims env vars defensively so it can't recur silently. "Powered by FatSecret" + Open Food Facts (ODbL) attribution added in You.

**Real bugs and findings from actually running this, not from reading the code:**
1. **RED-S floor over-elevated ordinary targets.** Fixed to 30×FFM alone; see PRD Appendix step 3.
2. **Macros didn't sum to the stated calorie total** during a cut. Carbs now compress to fit the budget; protein and the fat floor never move. See PRD Appendix step 4.
3. **Every exercise's sets showed the same seed weight** regardless of which exercise. Fixed.
4. **A single-data-point bodyweight chart** rendered as an invisible axis. Added an explicit empty state.
5. **Open Food Facts silently blocks generic User-Agent strings** — returns an HTML bot-wall page instead of an error. Not documented loudly anywhere; found by trial and error against the live API.
6. **Open Food Facts has no locale awareness by default** and skews heavily French/European — an English "chicken breast" query returned almost entirely French-labeled products until scoped with a country filter (`Secrets.foodDatabaseCountryFilter`, set to `"canada"`). Even scoped, OFF's Canadian coverage is thin for generic categories — USDA's US-branded results (many sold in Canada too) end up filling that gap, which is an honest reflection of real data availability, not a bug to chase further.
7. **A test-harness bug, not a product bug**: the mock network layer for food-database tests (`MockURLProtocol`) used one shared static dictionary, and Swift Testing parallelizes across suites by default — two tests stubbing the same URL fragment raced and clobbered each other intermittently. Fixed by consolidating the network-dependent tests into one `.serialized` suite; confirmed stable across 5+ repeated runs.

**PostHog (FRG-003)**: live and verified — `PostHogSDK` initializes with the real Project API Key in `App/ForgeApp.swift`, confirmed via a real network trace in Simulator (TLS handshake + HTTP 304 from `us-assets.i.posthog.com`, not just a compile check). Two Goal 05 events instrumented so far: `onboarding_completed` (program + goal chosen) and `progress_viewed` (history-depth engagement). More events can be added the same way as other Goal 05 signals (e.g. AI-convenience feature usage) become buildable.

**FRG-301, FRG-304, FRG-306, FRG-307 (P1 fast-follows)** — no wireframes existed for any of these (the 9-screen wireframe set is P0-only); built to match the existing app's visual language instead:
- **FRG-301 weekly recalibration**: `WeeklyRecalibrationEngine` compares actual weight trend (from `bodyweightLogLb`) against the trend the goal's TDEE adjustment implies, and nudges the baseline calorie target to close the gap — damped and capped the same way Load Score's daily swing is. Needs 4+ weigh-ins across a 14-day window before it activates (otherwise a single noisy weigh-in could swing the target). Surfaced as its own line in the Target Explanation sheet.
- **FRG-304 Apple Health sync**: `HealthKitManager` reads steps and sleep (read-only, `com.apple.developer.healthkit` entitlement + `NSHealthShareUsageDescription`). Toggle in You; confirmed the entitlement doesn't break install/launch in Simulator. FRG-305 (the actual Load-Score sleep dampening) is a separate, still-unbuilt ticket — this only makes the data available.
- **FRG-306 logging reminders**: `ReminderManager` schedules local notifications (7pm workout / 8pm meal), cancelled the moment the user actually logs — no server/push infrastructure needed.
- **FRG-307 CSV export**: `CSVExporter` + `ShareLink`. Nutrition and per-exercise detail are limited to today — the in-memory store doesn't persist multi-day detail yet (that's what CloudKit persistence, FRG-130/131, will unlock); historical bodyweight and per-session volume load already export in full.

**CloudKit persistence (FRG-130/131)** — live and verified end-to-end against a real container, not just compiling:
- `CloudKitStore` (private database) persists profile+program, workout session history, bodyweight log, and today's food diary. `AppStore`'s `@Published` state is still the UI's only source of truth (no view code changed) — mutating methods update it immediately and fire a background CloudKit write.
- Closed two real gaps this uncovered: there was previously no UI path that ever logged a second bodyweight entry, and no "finish workout" action that ever archived a session into history — both fixed (`+ Log weight` in Progress, `Finish Workout` in Train), since CloudKit persistence would otherwise have had nothing real to sync.
- Returning users with a saved profile skip onboarding entirely (`RootView` checks CloudKit before deciding which screen to show) — confirmed live: profile save → app relaunch → onboarding skipped.
- Bundle ID is `com.epworthlabs.forge` (changed from `com.forge.app` — that iCloud container identifier was already taken globally, since Apple's iCloud namespace spans every developer, not just one team).
- Live-verified via real device signing (Team `88XVUU2829`) + a real iCloud account signed into Simulator: OAuth-style "no account" error → resolved after Simulator iCloud sign-in → profile fetch/save confirmed working via Simulator log capture (`CKFetchRecordsOperation` succeeding, zero errors). Still to verify once weight-logging/finish-workout are exercised more: the WorkoutSession/BodyweightEntry/FoodEntry record types need their `date` field marked Queryable (and Sortable for BodyweightEntry) in the CloudKit Dashboard's Development schema the first time those types get created by an actual save — CloudKit doesn't auto-index custom fields for query predicates.

**Ticket cleanup pass (2026-07-20)** — real gaps found by reading the actual code against backlog tickets, not by trusting stale "done" tags left over from before this session:
- **FRG-111 rest timer**: was a static Int set once and never decremented — not a real timer. Rebuilt on a stored end `Date` + `TimelineView`, so it's always correct even after backgrounding.
- **FRG-112 previous-session pre-fill**: `ExerciseSlot.lastPerformance` existed but nothing ever populated it. Now backfilled from training history on load/after finishing a workout, and actually displayed in Train ("Last time: …").
- **FRG-113 progressive-overload suggestions**: new `ProgressiveOverloadEngine` in ForgeCore — +5% (rounded to nearest 2.5kg plate) on hitting target reps at RPE ≤8, hold at higher RPE, −10% on missed reps. Surfaced as an Accept/Dismiss card in Train, never auto-applied.
- **FRG-221 PR history**: replaced a hardcoded "Back Squat: 225×5" placeholder in Progress with real per-exercise records from training history. Needed a foundational fix first — `SetLog` had no exercise identity at all (Load Score math never needed it), so historical sets couldn't be attributed to a specific lift.
- **FRG-222 weekly adherence**: "workouts completed" was already real; "target hit: 5/7 days" was hardcoded. Now reconstructs each of the last 7 days' Load Score from sessions strictly before that day and compares against that day's actual logged nutrition (approximates with today's profile, since historical profile snapshots aren't persisted).
- **FRG-206** clarified, not changed: the missed-session half is already covered by the existing math (zero volume on an unlogged day naturally pulls Load Score toward 0); "scheduled deload weeks" specifically is still open since it needs a real weekly program schedule that doesn't exist yet.

**Today screen skinned with "Liquid Glass"** — the design exploration's converged-on direction (frosted blur cards, indigo/teal accent, ring charts), applied to `TodayView` only per explicit scope (other screens still use the original look). Palette is an exact sRGB conversion of the design file's oklch tokens via the standard OKLab matrices, not eyeballed — SwiftUI on iOS 16 has no native oklch support. Verified live in Simulator, light and dark.

**FRG-104 custom program builder** — required a foundational fix first: `ProgramTemplate` previously carried no exercise content at all (just id/name/daysPerWeek/weeks), so `AppStore` hardcoded the exact same 3 exercises (Squat/RDL/Leg Press) regardless of which program was selected — program choice was cosmetic. Redesigned around `ProgramDay`/`ProgramExercise` (see `App/Store/Program.swift`); the 3 curated templates now have real day-by-day content (exercise names verified against the bundled 873-exercise dataset, not guessed), and `todaysExercises` is actually built from `program.days`. Programs with multiple days rotate by session — advancing on Finish Workout, not tied to a calendar day, since there's no weekly-schedule concept. `CustomProgramBuilderView` lets a user name a program, add days, and search the same 873-exercise library per day with sets/reps/weight — the result is a plain `ProgramTemplate`, indistinguishable from a curated one past selection. Persists through the same CloudKit path as everything else (`programDaysJSON` + `currentProgramDayIndex` on the Profile record); old pre-this-change records fall back to an empty `days` array gracefully rather than crashing (confirmed live — this session's own test account, saved before the redesign, correctly showed an empty exercise list rather than crashing).

**Round two of ticket cleanup (2026-07-20)** — closing out the rest of Phase 2's launch gate plus FatSecret:

- **FatSecret (FRG-122) fully live** — Premier-Free approved (unlocking CIDR-range IP allowlisting), proxy deployed on Render, real search results confirmed flowing into the app. One real bug found and fixed along the way: `FATSECRET_CLIENT_ID` had a leading space in Render's env var — invisible in the dashboard, silently broke the OAuth Basic-Auth header. Found via a temporary diagnostic endpoint (removed once fixed); the proxy now trims env vars defensively so it can't recur silently. "Powered by FatSecret" + Open Food Facts (ODbL) attribution added in You.
- **FRG-303 scrapped**, not deferred — decided not to build the AI photo meal estimate at all; barcode scan + manual search/entry cover the need.
- **FRG-305 sleep modifier**: new `SleepModifier` in ForgeCore dampens Load Score's positive contribution on <7h sleep nights (using FRG-304's HealthKit read), never adds calories to compensate. The Target Explanation sheet deliberately shows the true undampened Load Score as its own line — same transparency principle as breaking out weekly recalibration, so "what did I actually train today" and "how did sleep affect the target" stay legible as two separate facts.
- **FRG-206 (deload weeks)**: `ProgramTemplate` gained an optional `deloadEveryNWeeks` (set to 4 for the 5/3/1 curated template, matching its classic 3-weeks-up/1-week-down structure); `AppStore` tracks program start date and forces Load Score down during a scheduled deload week regardless of trailing volume, surfaced as a badge in Train.
- **FRG-114 offline sync queue**: every CloudKit write previously used `try?` and silently dropped on failure — no retry, real data loss in a genuine gym-dead-zone scenario. New `SyncQueue` actor persists failed writes to disk (survives force-quit while offline, not just backgrounding) and flushes on network restore (`NWPathMonitor`) and app foreground. Verified: builds and launches clean. Not verified: an actual offline→online transition, since Simulator has no reliable network-kill switch via `simctl` — this one is reviewed-correct, not live-tested the way other network features this session were.

**Round three — user-requested features (2026-07-20)**:

- **FRG-127 editable portions**: food search/barcode/recent-food selection now goes through a portion-confirmation sheet (0.25× step) before logging, scaling macros proportionally, instead of logging immediately on tap.
- **FRG-115 mid-session exercise editing**: Train can now swap an exercise (session-only substitution, re-seeds weight from that exercise's own history if any exists), remove an exercise, add one from the full 873-exercise library, remove individual sets, and edit weight/reps directly — none of which touches the program definition itself. That distinction matters: a one-off "gym doesn't have this machine today" swap shouldn't silently rewrite the program going forward, which is what the separate program editor (below) is for.
- **FRG-104 second pass — editable timeframe + per-week customization**: `ProgramTemplate` restructured around `weekCount` (the editable timeframe) + `defaultDays` + a sparse `weekOverrides` dictionary, so a program can repeat the same split every week (no storage overhead, the common case) or have individual weeks customized, with "copy this week to all future weeks." `ProgramEditorView` (renamed from the original onboarding-only builder) now handles both creating a new program and editing an existing one from Train's new "Edit Program" button. **Breaking change to the CloudKit schema**: the Profile record now stores the whole `ProgramTemplate` as one JSON blob instead of exploded fields — simpler given the new nested structure, but pre-existing saved profiles (anything saved before this change) won't decode and will fall back to onboarding again rather than partially working. Confirmed this is exactly what happens (no crash) against this session's own test account, which predates the change.

**Round four — five UX notes (2026-07-20)** (FRG-308–312):

- **FRG-308 lb by default**: Train, the program editor's weight stepper, PRs, and last-performance strings all display/edit workout weight in lb now via a new `WeightUnit` helper. Internal storage stays kg (`SetLog.weightKg`, `ExerciseSlot.targetWeightKg`) — `ProgressiveOverloadEngine`'s ±5%/10% plate math and the CloudKit schema are untouched, this is purely a display/input-layer conversion. Body weight (`UserProfile.weightKg`, TDEE math) was already lb-in/kg-stored via the same pattern from onboarding — this just extends it to lifting weight.
- **FRG-309 free-form portions**: `PortionConfirmSheet` replaced the 0.25×-serving stepper with a typed amount in g, oz, or servings. `FoodSearchResult.referenceGrams` parses a gram (or ounce) figure out of `servingDescription` where one exists — true for the common case across all three sources (USDA/OFF/FatSecret mostly report "per 100g" or "per Xg serving") — and the sheet falls back to servings-only (still freely typed, not step-locked) when it can't.
- **FRG-310 search ranking**: `FoodSearchService.search` now scores results after the existing merge/dedup — unbranded results and closer name matches rank higher, unless the query itself names a brand (substring match against `brand`), in which case that brand wins. Source-merge priority (OFF→USDA→FatSecret, for dedup precedence) is unchanged; only display order changed.
- **FRG-311 Progress training history**: new collapsed-by-default "Training History" `DisclosureGroup` in Progress — a hand-rolled monthly calendar (no third-party dependency) marking days with a `trailingSessions` entry, plus a per-lift chart (exercise picker + `Chart`/`LineMark`) of the heaviest set per session for that lift. Deliberately not Swift Charts' `SectorMark`/donut-style APIs where avoidable — those need iOS 17 and this app's deployment target is 16; the calendar and macro donut below are both custom `Canvas`/`Path` work instead.
- **FRG-312 Eat collapsible + macro donut**: each meal section in `EatView` collapses independently (chevron toggle, state kept in a local `Set<Meal>`). Added a donut chart of today's logged protein/carb/fat calorie share with % labels in the legend, drawn via `Canvas` + `Path.addArc` (not `SectorMark`, same iOS 16 constraint as above) — recomputed live from `store.totals()` on every food log.

All five: `swift test` (38/38 passing, unchanged) + `xcodebuild` (BUILD SUCCEEDED) + fresh install/launch confirmed clean in Simulator. Not yet tap-through verified live — that needs a pass through onboarding since the FRG-104-second-pass CloudKit schema change (round three, above) already invalidates any previously saved profile on this test account.

**FRG-101 — real body-stat onboarding inputs (2026-07-20)**: age, sex, and height were hardcoded placeholders (30 / male / 178cm) baked into the profile at the moment onboarding finished — every BMR/TDEE calculation for every user who wasn't a 30-year-old 178cm male started from the wrong baseline, silently. Onboarding is now 5 steps instead of 4: a new "About you" step (age + sex + height) ahead of the existing weight/activity step. Age and height use scrollable wheel pickers (`.pickerStyle(.wheel)`), not the old ±5 stepper-button pattern — every value is directly reachable, nothing gets skipped stepping through. Weight's ±5 lb buttons were replaced the same way. Height and weight both have an editable unit toggle (ft/in ⟷ cm, lb ⟷ kg) that converts through a single canonical value (`heightCm`, `weightLb`) rather than tracking parallel unit-specific state. Sex is asked as a required, un-defaulted choice (`Sex?`, gates the step's Continue button) since a silently-wrong guess there is a real error in the calorie math, not a reasonable default the way a wheel's starting position is — labeled "Sex" rather than "Gender" since it maps directly to the Mifflin-St Jeor formula's male/female offset term and there's no third term in that formula to back a third option honestly. Verified: `swift test` (38/38) + `xcodebuild` BUILD SUCCEEDED + fresh launch confirmed the new step renders correctly in Simulator (screenshot-checked: wheel pickers scroll, unit toggle switches ft/in↔cm live, Continue stays disabled until Sex is picked). Not yet tap-through verified for the full onboarding completion path.

**Round five — five more notes (2026-07-20)** (FRG-313–316, FRG-125 removed):

- **FRG-313 You tab methodology doc**: new "How your numbers are calculated" row opens `CalorieMethodologySheet` — a static walkthrough of the full BMR→goal-adjustment→weekly-recalibration→Load-Score→sleep→RED-S-floor→macro pipeline, separate from Today's `TargetExplanationSheet` (which shows *today's actual numbers* run through the same pipeline, not the methodology itself).
- **FRG-125 barcode scanning removed**, not just descoped — explicit decision. Deleted `BarcodeScannerView.swift`, removed the scan button/sheet from `FoodSearchView`, removed `AppStore.lookupBarcode`. Left `FoodSearchService`/`OpenFoodFactsClient`'s `lookupBarcode` in ForgeCore alone — it's a self-contained, tested engine capability independent of the UI feature, and nothing forces its removal just because nothing calls it right now.
- **FRG-314 apply Train edits to future weeks**: swap/remove/add-exercise and finishing a sets/reps/weight edit now prompt "Also update your program for this week and beyond?" New `AppStore.applyTodaysChangesToFutureWeeks()` promotes today's (possibly edited) exercise list into the program itself, replacing just today's day slot from the current week forward — past weeks are left alone since they already happened. Declining keeps the previous session-only behavior (FRG-115) exactly as it was.
- **FRG-315 visible "Done Editing" button**: exiting set-editing mode required reopening the ⋯ menu before; now there's a prominent in-card button while editing, which also triggers FRG-314's prompt.
- **FRG-316 custom exercises**: "Can't find it? + Add your own" in the exercise picker (shared by Train's add/swap and the program editor). Needed a real ForgeCore gap closed first — `Exercise` had no public initializer, so App-layer code couldn't construct one at all (Swift's synthesized memberwise init for a public struct is only `internal` without one). New local-only `CustomExerciseStore` (Application Support JSON, same pattern as `SyncQueue`'s pending-write file, not CloudKit-synced — device-scoped for now). `AppStore.buildExerciseSlots` now resolves an exercise name against both the bundled library and the custom store, so a custom exercise survives program rebuilds (Finish Workout, program edits) instead of silently dropping out of the day.

All five: `swift test` (38/38) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean. Onboarding's first step screenshot-reconfirmed working after these changes; the Eat/Train/You changes in this round are build-verified only, not tap-through verified yet.

**Round six — Train section restructure (2026-07-20)** (FRG-317–321):

This was the biggest structural change to Train since FRG-104: the tab's root used to jump straight into logging today's exercises; it's now a funnel — `ProgramSelectionView` → `DaySelectionView` → `TrainSessionView` (the old logging screen, unchanged in substance) — with `WorkoutCompleteView` as a full-screen cover after Finish Workout that hands back into day selection.

- **FRG-317 multi-program library**: `AppStore` gained `savedPrograms: [ProgramTemplate]` — previously there was exactly one `program` a user could ever have, edited in place. `activateProgram(_:)` switches to a different program or activates a freshly-built one (resets to week 1/day 0 — "start this program," distinct from `updateProgram`'s in-place edit of progress already made). `ProgramSelectionView` is a tile grid of the library plus "+ New Program," and is now genuinely the Train tab's entry point. CloudKit schema gained `savedProgramsJSON` — additive this time: old records without it just default to a single-program library built from whatever `programJSON` already had, so unlike the two prior schema changes this session, nothing here forces existing accounts back through onboarding.
- **FRG-318 day selection**: new screen listing the current week's days as tiles, with whichever day Finish Workout most recently rotated to marked "Suggested." Also where the "review the workout you just finished" entry point lives (`AppStore.lastCompletedSession`, cleared once `selectDay` starts something else).
- **FRG-319 congratulatory screen + review**: `WorkoutCompleteView` (exercise/set/volume stats pulled from `lastCompletedSession`, `UINotificationFeedbackGenerator` success haptic) shows as a `.fullScreenCover` after Finish Workout — previously finishing just silently reset the same screen. `SessionReviewView` lists every set of that session grouped by exercise.
- **FRG-320 rest timer**: `restSecondsRemaining` no longer clamps at 0 — it goes negative, and the rest card renders it as "+M:SS" in red past zero, with a one-time warning haptic fired via `.task(id: remaining)` (runs once per distinct per-second value, so it can't double-fire the way a hand-rolled `@State` flag might under re-renders). Duration is now user-editable (pencil icon → wheel picker, 15s steps, 0–10min), persisted in `UserDefaults` as `AppStore.restDurationSeconds` — a local display preference, not CloudKit-synced, same tier as dark mode.
- **FRG-321 numpad program entry**: sets/reps/weight in the program editor, plus the timeframe week count, switched from Steppers to digit-filtered numeric-keypad `TextField`s clamped to range as you type.

All five: `swift test` (38/38, ForgeCore untouched this round) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean. This is the largest navigation change of the session (new `NavigationStack`-based flow, four new files under `App/Features/Train/`) — build-verified only, not tap-through verified; worth a full walk of Train (pick a program, pick a day, finish a workout, see the congrats screen, review it, edit rest duration) before trusting it live.

**Round seven — Workout/Progress refinements, real calorie targets, full-app skin (2026-07-20)** (FRG-322–326):

- **FRG-322 future-weeks prompt scope**: refines round six's FRG-314 — no longer fires after finishing a sets/reps/weight edit, only after swap/add/remove exercise. "Ask... don't do this for edits" — a numbers tweak on an exercise already on the day isn't the same kind of change as altering which exercises are there.
- **FRG-323 numpad + quick-adjust for Train weights**: `EditableSetRow`'s weight control gained a numeric-keypad `TextField` alongside (not instead of) the existing ±5lb buttons.
- **FRG-324 Lift Progressions replaces Recent PRs**: the flat PR list is gone from Progress; the per-lift chart (previously buried in a collapsed "Training History" disclosure) is now the primary, always-visible section. `AppStore.personalRecords()` removed — nothing called it anymore. The workout calendar stays collapsed on its own, relabeled "Workout Calendar."
- **FRG-325 target-weight/timeframe-driven calorie goal** — the most substantive change this round: onboarding's goal step (cut/bulk only) now asks for a target weight and a time period. `TDEECalculator.goalAdjustedTDEE` derives the daily deficit/surplus directly from those (7700 kcal/kg, same constant `WeeklyRecalibrationEngine` already used) instead of Goal's fixed ±20%/+12.5%, whenever both are set. Critically, this is anchored to `profile.weightKg` *as it was when the target was set* — not today's logged weight, not "weeks remaining until a date" — which is what makes it satisfy "should not change unless these settings are changed in the app": the derived number is fixed until goal/target/timeframe are explicitly edited again, not silently drifting with new weigh-ins or the calendar. The daily Load Score swing and weekly recalibration still layer on top of this baseline exactly as before — the "connected engine" premise is untouched, only how the *baseline itself* is derived changed. `UserProfile` gained `targetWeightKg`/`targetWeeks` (ForgeCore, both optional — additive, doesn't disturb the CloudKit schema). New `AppStore.updateGoalAndTarget` + a `GoalTargetEditSheet` wired to You's "Goal & Target" row (previously a dead "Edit Preferences" row that didn't do anything). 5 new ForgeCore tests (`TargetDrivenCalorieTests`) — 43 total now. One deliberate scope call: maintain/recomp don't get a target-weight prompt at all, since there's no literal weight target for either — they keep the existing fixed 0% adjustment.
- **FRG-326 skin the whole app in Liquid Glass**: the reskin from earlier this session (Today only, deliberately kept separate from the shared `GlassCard` at the time — see git history) is now everywhere. `LiquidCard`/`RingStat`/`LiquidTile`/`LiquidMacroRow` moved out of `TodayView.swift` into shared `App/DesignSystem/LiquidComponents.swift`; the shared `GlassCard` (already used by every screen) now *is* the Liquid Glass treatment, so Eat/Progress/You/Train inherited the new look with zero call-site changes beyond `TodayView` itself switching its own `LiquidCard` calls to `GlassCard`. Every remaining screen's flat `ForgeColors.backgroundBase.ignoresSafeArea()` became the three-stop gradient `backgroundWash`, including the launch loading screen. New shared `DashedActionButton` replaces a few duplicated "+ Add ___" button implementations (Train, onboarding's custom-program entry point).

All five: `swift test` (43/43) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean, and the gradient wash + frosted onboarding cards visually confirmed live via Simulator screenshot. Functional pieces (future-weeks scoping, numpad weight entry, Lift Progressions, target-driven calories) are build-verified only, not tap-through verified.

**Round eight — small fixes (2026-07-20)** (FRG-327, FRG-328):

- **FRG-327 cut "training days per week"**: audited (per a direct question — "what's the point of having training days per week in the onboarding screen?") and confirmed it was dead: `trainingDaysPerWeek` was set by that step and read only by its own UI, never passed to `buildProfile()`, never stored on `UserProfile`, never used to filter or recommend a program. Removed the step and the property outright rather than wiring it up after the fact — onboarding is 4 steps now (was 5).
- **FRG-328 Progress fixes**: Lift Progression (FRG-324) is now collapsible (`DisclosureGroup`, defaults open). Real bug found and fixed in the workout calendar: `WorkoutCalendarView.weekdaySymbols` used `Calendar.veryShortWeekdaySymbols`, which is always Sun–Sat regardless of locale — but the day-cell grid already correctly shifts its leading blank cells by `calendar.firstWeekday`. On any locale where the week starts Monday (most of Europe, among others), the S/M/T/W/T/F/S header was lined up with the wrong columns. Fixed by rotating the header symbols to match `firstWeekday` the same way the grid already does.

`swift test` (43/43) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean.

**Round nine — Progress/Eat polish + complete program templates (2026-07-21)** (FRG-328–330):

- **FRG-328 workout calendar reverted to non-collapsible**: explicit follow-up — "don't make the workout calendar collapsable, keep it fixed." It's back to always-visible (Lift Progression stays the only collapsible section on the tab, per the immediately preceding request).
- **FRG-329 food row monogram**: the icon slot next to every food row (search results, logged meal entries) was an empty flat-color box — no image ever loaded into it, since none of USDA/Open Food Facts/FatSecret return one. Replaced with a letter-monogram avatar, matching the pattern already used for the profile row in You. New shared `FoodMonogram`.
- **FRG-330 complete program templates**: the 3 curated templates looked unfinished because they were — 5/3/1 was literally one lift per day, PPL and Upper/Lower had 3 exercises where a real version of either runs 5-6. Rebuilt as 4 templates aimed at different classes of gym-goer: **Beginner Full-Body** (new — 2-day A/B rotation, the app's day-rotation-on-Finish-Workout naturally turns that into a 3x/week A-B-A / B-A-B pattern), **Push/Pull/Legs — Hypertrophy** (bodybuilding-style volume, isolation work added), **5/3/1 for Strength** (Boring-But-Big-style assistance work behind each main lift — real 5/3/1 isn't one set of 5 and done), **Upper/Lower — General Fitness** (balanced 4-day intermediate split). Every exercise name re-verified against the bundled 873-exercise dataset via script before writing, same discipline as the original 3.

`swift test` (43/43) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean.

**FRG-331 — Sign in with Apple (2026-07-21)**: scoped deliberately to Apple only, not Apple+Google+email, after laying out the tradeoff — this app's whole data layer lives in CloudKit's private database, already scoped to whichever Apple ID the device is signed into. Google or email sign-in would each need an entirely separate backend (CloudKit has no non-Apple identity concept), which is a real rearchitecture, not a login-screen feature; deferred as a future decision rather than built speculatively. New `com.apple.developer.applesignin` entitlement; `AppleSignInManager` stores the Apple user identifier and re-validates it against Apple's servers on every launch via `ASAuthorizationAppleIDProvider.getCredentialState` (catches a revoked sign-in, not just a locally-cached "signed in once" flag); `SignInView` (`SignInWithAppleButton`) gates `RootView` ahead of the existing CloudKit-fetch-or-onboarding flow. **Update, same day**: disconnected again — the entitlement started blocking Simulator builds mid-development (a real Xcode signing/provisioning saga: "requires a development team" → a phantom missing `.mobileprovision` reference once a team was picked). Removed the entitlement from `project.yml` and the gate from `RootView`; the `AppleSignInManager`/`SignInView` code stays in place, just unwired, with a comment at the removal site explaining exactly how to re-enable both pieces pre-launch.

**Round ten — Train/Progress/You bug fixes, macro customization, avatar, bigger touch targets, rename (2026-07-21)** (FRG-332–342): the largest single-message batch this session — 6 real bugs, 2 new customization features, and an app-wide touch-target/rename pass.

- **FRG-332 future-weeks prompt, real bug fix**: it never actually appeared for swap or remove, despite looking correctly wired. Root cause: both mutate `todaysExercises`, and `ExerciseSlot.id` is a fresh `UUID()` generated at struct-init time — `swapExercise` was replacing the array element with a brand-new `ExerciseSlot(...)` value, silently giving the swapped-in exercise a *different* SwiftUI identity than what it replaced. `ForEach` diffs by that id, so SwiftUI tore down the old `ExerciseCard` (and the `showFutureWeeksPrompt` state that was about to flip true on it) and mounted a fresh one in the same render pass, before the dialog ever got a chance to show. `removeExercise` has the same problem for a more obvious reason — the view is just gone. Fixed two ways: `swapExercise` now mutates the existing array slot's fields in place (preserves `id`), and the prompt's state moved up to `TrainSessionView` (the parent, never destroyed by either mutation) instead of living on each card. Also: "change the prompt... to an actual window" — swapped `.confirmationDialog` for a real `.alert`.
- **FRG-333 weights default to clean 5lb increments**: "I don't want to see 121lbs." The curated templates were authored in kg (55kg → 121lb, ugly); all 66 weight values across the 4 templates were re-derived via script from round-5lb targets, and `AppStore.buildExerciseSlots` now also snaps any program's `targetWeightKg` to the nearest 5lb-equivalent as a safety net (custom programs, future templates). New `WeightUnit.roundedToNearestFiveLb`.
- **FRG-334 progressive-overload rounds 5lb/2.5lb by equipment**: `ProgressiveOverloadEngine.suggestNextSet` gained a `roundingIncrementKg` parameter (default unchanged at 2.5kg, so existing tests didn't need to move); `AppStore.suggestion(for:)` passes 5lb-equivalent for barbell work, 2.5lb otherwise, keyed off `Exercise.equipment`.
- **FRG-335 numpad UX overhaul**: "I don't want to have to select the field, I just want it to be inputtable immediately" + "it should replace the existing numbers, I don't want to have to select numbers like I'm typing." New shared `NumpadField` clears itself on focus (SwiftUI's `TextField` has no text-selection API on iOS 16 without dropping to UIKit, so "clear and let them retype" is what actually delivers "replaces, doesn't append"), clamps to a max digit count (2 for sets/reps, 3 for weight), and is sized to a real 44pt target. Replaces the one-off numpad fields in Train and the program editor. New `IconButton` does the same for the small circular ±/pencil/trash controls.
- **FRG-336 & FRG-337 Progress metrics, both were hardcoded placeholders that never updated**: `workoutsCompletedThisWeek`/`workoutsPlannedThisWeek` were literally `4`/`5`, forever — "pull the workouts completed metric based on the active workout program... if it's 3x a week and completed 1 of 3, that's 1/3" — now computed from `program.daysPerWeek` and real session dates this calendar week. "Target hit" (a binary 7-day hit/miss count) is replaced with `AppStore.nutritionWeekSummary()` — days logged, average calories vs average target, and an actual over/under/on-target percentage.
- **FRG-338 dark mode toggle, real bug fix**: `.preferredColorScheme` was applied only on `YouView`'s own subtree — never touched the other four tabs, and didn't reliably override the raw `UIColor` dynamic providers `ForgeColors` uses (those resolve off the real UIWindow trait collection). It looked "perpetually dark" because the app was just following the Simulator's system appearance regardless of the toggle. Moved to the true root (`RootView`), and switched from dark-vs-follow-system to an explicit dark-vs-light override — the right semantics for a single on/off switch. Verified with a clean uninstall/reinstall (fresh `UserDefaults`, no stale toggle state left over from earlier testing): light by default, as expected.
- **FRG-339 manual macro split**: "protein intake seems a bit low, let users adjust the target macro splits manually." You → Goal & Target gained a "Custom macro split" toggle — protein %/carb % set directly, fat % derived as the remainder. `UserProfile` gained `manualProteinPercent`/`manualCarbPercent`/`manualFatPercent` (percentages, deliberately not fixed grams — grams would fight the daily Load-Score-driven calorie swing, percentages scale with it); `NutritionTargetEngine.calculate` uses them when all three are set, falls back to the goal-based default otherwise. 2 new ForgeCore tests.
- **FRG-340 editable avatar + random username**: the profile row was a hardcoded "Alex Rivera" + flat circle. Now a random adjective-noun-number username on first launch, a default silhouette avatar, and a `PhotosPicker`-backed edit sheet for both — local-only (`ProfileSettings`), presentation data rather than the core domain data CloudKit already owns.
- **FRG-341 bigger touch targets**: a pass across Train, Progress, Eat, and the program editor raising icon-only buttons to the 44pt HIG minimum and widening several row/button paddings.
- **FRG-342 renamed to Trakt**: display name only (`PRODUCT_NAME`/`CFBundleDisplayName`) — bundle identifier and the CloudKit container stay `com.epworthlabs.forge`/`iCloud.com.epworthlabs.forge`, both internal and invisible to users; renaming either for real would mean a new CloudKit container and re-provisioning. Also dropped a stale `NSCameraUsageDescription` left over from the removed barcode scanner (FRG-125) — an unused declared permission is exactly what App Store review flags.

`swift test` (46/46) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed clean, dark-mode fix specifically re-verified via clean uninstall/reinstall to rule out stale `UserDefaults` masking the result.

**How logging reminders, Apple Health sync, and data export actually work** (asked, answered here rather than as a code change):
- **Logging reminders** (`ReminderManager`, You tab toggle): local notifications only, no server/push infra. Turning it on requests notification permission once; `AppStore.refreshReminders()` (called on toggle-enable and every app backgrounding) schedules a 7pm "log your workout" and 8pm "log your food" local notification *only* for whichever of those two you haven't done yet that day, and cancels each one the moment you actually do it (`toggleSet`/`logFood` call `ReminderManager.shared.cancelWorkoutReminder()`/`cancelMealReminder()` directly). Nothing fires if you've already logged both.
- **Apple Health sync** (`HealthKitManager`, You tab toggle): read-only HealthKit authorization for steps and sleep. Enabling it requests the OS permission dialog once, then calls `AppStore.syncHealthKit()`, which populates `stepsToday`/`lastNightSleepHours` — these feed FRG-305's sleep-based Load Score dampening (poor sleep pulls back an already-elevated day, never adds calories) and are shown as a small "steps today / sleep last night" readout right under the toggle. It's a fresh query each time (on toggle-enable, and again on every YouView visit if already on), not a live subscription — closing and reopening the app is what refreshes it, not a background push.
- **Export data (CSV)** (`CSVExporter`, You tab): builds CSV files in memory and hands them to the system share sheet (`ShareLink`) — AirDrop, Files, Mail, whatever the user picks. Bodyweight history and per-session workout volume export in full; nutrition/exercise-level detail is limited to *today* specifically, since the in-memory store doesn't hold multi-day food/exercise detail beyond what's already loaded from CloudKit — historical CloudKit records exist, but this exporter doesn't currently page back through them.

**FRG-343 — app icon + in-app logo (2026-07-21)**: "logo and app icon are in claude design, retrieve and skin the app icon." Used the `claude_design` MCP to pull the actual approved design from the project's `App Icon and Logo Options.dc.html` file rather than guessing or redesigning — the doc's most recent turn was explicitly labeled "Finalists," pointing at option **5a** (a bold dual-ring mark — overlapping "training + nutrition" rings — on a deep indigo gradient tile) for the icon and **5b** (heavy-weight, wide-tracked "TRAKT" wordmark) for the logo.

- **Icon**: converted the design's OKLCH color tokens to exact sRGB using the same OKLab→linear-sRGB→sRGB matrices `ForgeColors.swift` itself was derived with earlier this session — notably, the wordmark's ink color converted out byte-identical to `ForgeColors.ink`'s existing light-mode hex, confirming the design was already built against this app's own palette, not a separate one. First tried rendering via a live browser screenshot of the actual SVG (would have been pixel-perfect against the source), but the Browser tool hung indefinitely on local `file://` URLs; fell back to a small Python/Pillow script replicating the SVG's exact geometry (circle centers/radii/stroke-widths/dasharray fractions scaled from the source's 108×108 viewBox) with proper alpha-composited stroke-width rings — first pass had a real bug (`ImageDraw.arc()`'s alpha fill wasn't blending against the gradient background, so the dim "track" arcs silently didn't render, making both rings look like solid crescents instead of the intended progress-ring look), fixed by drawing the rings on a transparent overlay and `Image.alpha_composite`-ing it onto the gradient. Wired into `Assets.xcassets/AppIcon.appiconset` (`ASSETCATALOG_COMPILER_APPICON_NAME` in project.yml) — this project had no asset catalog at all before now. Confirmed live on the Simulator home screen.
- **In-app logo**: new vector `TraktMark`/`TraktWordmark` (`App/DesignSystem/LiquidComponents.swift`) — SwiftUI shapes, not a raster image, so they scale cleanly at any size without asset bloat. Replaced the generic SF Symbol placeholder in the disconnected `SignInView` with the real mark + wordmark, and added a small lockup to onboarding's header. Screenshot-confirmed rendering correctly.

`swift test` (46/46) + `xcodebuild` BUILD SUCCEEDED + fresh install confirmed the real icon on the Simulator home screen and the in-app lockup on onboarding.

**FRG-344 — swap in the real, final icon/logo files (2026-07-21)**: "use these instead for logo and icon" — the user supplied the actual approved `icon.png` (360×360, dual-ring mark on a blue diagonal gradient) and `logo.png` (432×166, "TRAKT" wordmark on a white card) directly, superseding FRG-343's design-doc approximation with pixel-exact files.

- **Icon**: iOS requires a full opaque square with no pre-baked corner rounding, but the source file has transparent rounded corners — sampled the source's own corner pixel colors (top-left ≈ RGB(0,99,204), bottom-right ≈ RGB(0,102,180)) to regenerate a matching full-bleed 1024×1024 gradient, then alpha-composited a LANCZOS-upscaled copy of the source on top. Seamless, since the background was reconstructed from the source's own colors rather than guessed. Overwrote `Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png` directly.
- **In-app assets**: added `AppMark`/`TraktLogo` image sets (raw source PNGs, each with a `Contents.json`); `SignInView` and `OnboardingView` now use `Image("AppMark")`/`Image("TraktLogo")` instead of the vector approximation. Deleted the now-dead `TraktMark`/`TraktWordmark` SwiftUI structs from `LiquidComponents.swift` — no other call sites remained.

`swift test` (46/46) + `xcodebuild` BUILD SUCCEEDED + fresh install/launch confirmed both spots: the real dual-ring icon renders cleanly on the Simulator home screen (iOS's own mask rounds the corners, no seam from the composite), and the real mark + "TRAKT" wordmark render correctly in onboarding's header lockup.

**FRG-345 — revert Train/program-editor numeric entry to the pre-FRG-335 style (2026-07-21)**: "revert the way we edit the program template and input sets and reps back to what it was previously." FRG-335's shared `NumpadField` (clears-on-focus, digit-clamped) is still used elsewhere (You → Goal & Target's macro sliders), so it wasn't deleted — just stopped using it in the two places named: `TrainView.swift`'s `EditableSetRow`/`WeightNumberField` are back to the original small ±5lb circle buttons + append-style `TextField` for weight and a native `Stepper` for reps, and `ProgramEditorView.swift`'s Sets/Reps/Weight/Timeframe fields are back to the original private `NumberField` (restored verbatim from git history, same digit-filtering logic minus the clear-on-focus behavior).

**FRG-346 — referral wall on Lift Progression (2026-07-21)**: "place a referral wall on the lift progression section. I want to send the link out to one person in order to unlock this feature, if that person signs up, unlock this feature." No account system exists in the app (Sign in with Apple is disconnected, CloudKit is per-iCloud-account private data), and the app isn't distributed anywhere yet (no TestFlight/App Store listing), so there's nowhere for a real tappable link to open yet — clarified this with the user up front rather than guessing, along with where redemption state should live and what "signs up" means (answers: build the plumbing now and wire up real distribution later; extend the existing Render proxy rather than standing up new infra; "signs up" = completes onboarding).
- **Backend**: `FoodProxy/server.js` (same Render service already deployed for FatSecret) gained `POST /referral/redeem` and `GET /referral/status`, both behind the existing `X-App-Secret` header. Storage is an in-memory `Set` of redeemed codes — deliberately not a database; a free Render instance's memory resets on redeploy/restart regardless, and this is a single-friend test feature, not a scaled referral program.
- **App**: new `ReferralManager` (`App/Store/ReferralManager.swift`) generates a 6-character invite code per device (UserDefaults-persisted, no account needed) and polls `/referral/status` for that code. Progress's LIFT PROGRESSION card is gated behind `ReferralManager.isUnlocked` — locked state shows the code + a `ShareLink` with an invite message; unlocked state is the existing chart, unchanged. Onboarding's final step gained an optional "Invite code" field; entering one and finishing onboarding calls `/referral/redeem`.
- **Known gap, not yet done**: the new `/referral` endpoints are written but **not deployed** — confirmed via a direct `curl` against the live Render URL (`404`, not `401`), meaning the currently-deployed proxy predates this change. Redemption won't actually take effect until this is pushed and Render redeploys. The client fails silently (guarded `try?`) if the endpoints are unreachable, so the UI still renders and doesn't crash — screenshot-verified via a temporary, fully-reverted debug bypass (`git diff` confirmed clean after) since reaching onboarding's last step and the Progress tab both require multi-step taps this environment has no Simulator input-automation tool for.

`swift test` (46/46) + `xcodebuild` BUILD SUCCEEDED for both. FRG-345 also confirmed via fresh install/launch (Train and the program editor's numeric fields visually match the pre-FRG-335 style). FRG-346's UI confirmed via the temporary debug-bypass screenshots described above; the actual redeem→unlock round trip is not yet live-testable pending the Render deploy.

**App Store launch-readiness pass (2026-07-21)** — worked through the full punch list of what's needed before this can actually be submitted, not just feature-complete in Simulator:

- **Privacy manifest**: added `App/PrivacyInfo.xcprivacy` — Apple has required this since 2024 for any required-reason API usage. Declares `UserDefaults` (category `NSPrivacyAccessedAPICategoryUserDefaults`, reason `CA92.1` — own-app data only, confirmed by grepping for any file-timestamp/disk-space/boot-time APIs too, none found) and the app's actual collected data types (Health, Fitness, Product Interaction for PostHog), all `AppFunctionality`/`Analytics` purpose, none used for tracking.
- **Privacy policy + support URL**: both required by App Store Connect. Hosted as static pages on the same Render service already deployed for FatSecret (`FoodProxy/public/privacy.html`, `support.html`, served via new `/privacy` and `/support` routes) rather than standing up a separate site.
- **Real-device signing**: `project.yml` had `CODE_SIGN_STYLE: Automatic` but no team, so Automatic signing had nothing to resolve — this is what actually blocked archiving, not the Apple Sign-In entitlement that got blamed and disconnected earlier. Added `DEVELOPMENT_TEAM: 88XVUU2829` (the team already confirmed working for real-device CloudKit verification, FRG-130/131), plus explicit `MARKETING_VERSION`/`CURRENT_PROJECT_VERSION`.
- **Sign in with Apple re-enabled**: per the original plan ("we can add it back when we launch") — `com.apple.developer.applesignin` back in `project.yml`, `RootView` gates on `AppleSignInManager.isSignedIn` again. Doesn't block Simulator builds this time since the missing team ID (the actual cause) is fixed.
- **Nutrition/medical disclaimer**: added to onboarding's Goal step and the Target Explanation sheet — "estimates, not medical advice, talk to a doctor or registered dietitian."
- **Deload scheduling UI**: `ProgramEditorView` gained a "Scheduled deloads" toggle + interval field, so `ProgramTemplate.deloadEveryNWeeks` (FRG-206) is finally settable from a custom program, not just the curated 5/3/1 template's direct construction.

All verified via `swift test` (46/46) + `xcodebuild` BUILD SUCCEEDED + the temporary-debug-bypass screenshot technique used earlier for the referral gate (added, screenshotted, fully reverted — confirmed via `git diff` each time).

**Still needs a human, not more code:**
- **CloudKit Production schema promotion** — no tool access to the CloudKit Dashboard (needs your Apple ID login). Everything so far has run against the *Development* environment only. Before the first release build:
  1. Open [CloudKit Dashboard](https://icloud.developer.apple.com/dashboard/), select the `iCloud.com.epworthlabs.forge` container.
  2. In the **Schema** section (Development environment), confirm these record types exist (they're created automatically the first time the app saves one, so use the app in Simulator/device first if any are missing): `Profile`, `WorkoutSession`, `FoodEntry`, `BodyweightEntry`.
  3. On `FoodEntry.date`, add the **Queryable** index (used by a `date >=/<` range predicate).
  4. On `BodyweightEntry.date`, add the **Sortable** index (used by an ascending sort) — add **Queryable** too, since CloudKit predicates on unindexed fields fail outright rather than degrading.
  5. Use **Deploy Schema Changes** to push the Development schema (including these two indexes) to **Production**. This is a one-way promotion Apple doesn't let you undo from the dashboard — re-check the indexes above before confirming.
  6. Production and Development are entirely separate datastores — nothing you've tested in Simulator this whole session carries over. Do one real save-and-fetch smoke test against Production (e.g. a TestFlight build) before submitting.
- **App Store Connect listing** — screenshots, description, age rating: still needs a human in the Apple Developer portal. See the draft copy and screenshots below.

**Also needed before shipping, not before building:**
- Inter font files (Google Fonts, OFL license) aren't bundled yet — `ForgeType` falls back to the system font.
- Exercise demo images/GIFs aren't bundled — raise with design rather than pulling in an uncertain third-party asset set.
- The floating glassmorphic tab bar was simplified to a native `TabView` for now.
- `App/Config/Secrets.swift` is gitignored and currently holds real, working keys for USDA, FatSecret's proxy (deployed at `https://forge-food-proxy.onrender.com`), and PostHog. Copy `Secrets.example.swift` if `Secrets.swift` is ever missing.

**FRG-351 — food search timeout, real bug (2026-07-21)**: "the food items when I look them up are not very responsive, it takes a long time to load." Root cause: `FoodSearchService.search` queries USDA/Open Food Facts/FatSecret concurrently but awaited all three before returning anything — FatSecret's proxy (`FoodProxy/`, Render free tier) spins down after ~15 minutes idle, and a cold start can take 30-60s, which blocked the *entire* search that long even though USDA/Open Food Facts had already come back in under a second. Added a per-source timeout (`sourceTimeout`, defaults to 8s) via a `withTaskGroup`-based race between each source call and a timer — whichever finishes first wins, the loser is cancelled (URLSession honors cooperative cancellation, so a timed-out request is actually aborted, not left running). New regression test (`slowSourceTimesOutWithoutBlockingFasterSources`) uses a tiny injected timeout + a deliberately delayed mock response so it doesn't have to wait out a real 8s timeout; `MockURLProtocol` gained delayed-response support (`stubDelayed`) to make that possible. 47 ForgeCore tests passing (up from 46).

**FRG-352 — redesigned startup/sign-in screen from Desktop/Design.pdf (2026-07-21)**: replaced the old grey-card sign-in layout with the mockup's full-bleed blue gradient splash. Three explicit asks: implement the screen, add the logo (the mockup itself only had text), and drop "Continue with email."
- **Gradient**: sampled directly from the mockup's rendered phone-screen pixels (top ≈ RGB(36,139,213), bottom ≈ RGB(14,52,116)), not eyeballed — a vertical gradient, distinct from the app icon's own diagonal one.
- **Logo without its background**: the mockup only shows text, but the ask was to also add the ring mark — with its embedded blue-square background stripped out, so it sits directly on the new screen's own gradient rather than floating as a separate tile. Naively color-keying the existing `icon.png` against its known background left ghosting artifacts, because the ring art itself uses translucent "dim track" strokes (a lighter blend of white/teal over the blue) as part of the design — simple thresholding can't tell "faint ring" from "background." Fixed by solving per-pixel alpha analytically: for each pixel, find the best-fit blend against two known foreground colors (pure white, and the icon's actual teal `RGB(90,245,210)`, both sampled from the image itself) over the *local* expected background color, keep whichever candidate reconstructs the pixel more closely, and store the result as true straight alpha (color = the matched foreground, alpha = the solved blend amount) rather than baked-in RGB. That makes the dim track composite correctly against *any* background, including a completely different gradient. New `AppMarkTransparent` asset.
- **Continue with email**: not built — the screen only offers `SignInWithAppleButton(.signIn)`.
- **Update, same session**: the footer ("By continuing, you agree to our Terms and Privacy Policy") was removed entirely — asked whether a Terms page was actually required for App Store submission, answer is no (Apple's own Standard License Agreement applies automatically if none is provided), and the footer was making a claim ("you agree to our Terms") about a document that doesn't exist yet. Falling back to Apple's default rather than shipping a real-looking link to nothing.

`swift test` (47/47) + `xcodebuild` BUILD SUCCEEDED + fresh install screenshot-confirmed the gradient, logo, and button render correctly.

**FRG-353 — numpad clear-on-focus for weight/reps, reps gains typed entry (2026-07-21)**: "make the numpad entering more intuitive when editing weights and reps... I don't want to have to select the number when editing the field, just want the numpad to pull up and the field to edit when I start entering numbers, keep the formatting and everything else as is though." FRG-345 had reverted weight's field to plain append-style `TextField` (tap in, cursor lands mid-text, typing appends rather than replaces) and reps to a `Stepper` (no typing at all). Fixed narrowly, per "keep the formatting" — no return to FRG-335's bigger `NumpadField`/`IconButton` treatment:
- `WeightNumberField` gained the same clear-on-focus mechanism FRG-335 originally used (`@FocusState` + local `@State` text buffer, cleared when focus starts, restored from the real value if left empty on blur) grafted onto the *existing* small-±-button visual style — same size, same font, same "lb" suffix, nothing else changed.
- Reps had no typed entry at all before this — a `Stepper` can't support it. Replaced with a new `RepsNumberField` built to the identical pattern (same small ± circle buttons, same clear-on-focus text field, "reps" suffix) so weight and reps now read as one consistent control instead of two different ones.

`swift test` (47/47) + `xcodebuild` BUILD SUCCEEDED + screenshot-confirmed the row's layout (small ± buttons flanking a typed field, both weight and reps) via the temporary debug-bypass technique (added, screenshotted, fully reverted, `git diff` confirmed clean). The clear-on-focus *interaction* itself (tap in → field clears → type replaces) isn't independently verifiable in this environment — no Simulator input-automation tool exists here to actually tap into a field and type — but it's the exact mechanism FRG-335 used, which the user confirmed worked correctly earlier this session before being reverted for an unrelated reason (button sizing, not the clear-on-focus behavior itself).

**FRG-355 — real bug: fresh reinstall lost data even after signing back in (2026-07-22)**: "when I force quit the app the data stays, but when I delete and add, even when I sign in, I have to start fresh." Root cause: `RootView`'s CloudKit profile fetch was an unconditional `.task` that fired at launch *in parallel* with the new Sign in with Apple gate, not waiting on it — on a fresh reinstall, that fetch could resolve (fail, or simply return nil because the account/container wasn't ready yet) *before* the user finished actually signing in, permanently deciding "show onboarding" with no way to recover once `isSignedIn` later flipped true. Fixed by switching to `.task(id: signIn.isSignedIn)`, which only runs the CloudKit fetch once sign-in is actually confirmed, and re-runs it fresh every time sign-in state changes — no more racing a slower system flow. (Force-quit-and-relaunch never hit this bug, since sign-in was already resolved from a prior launch by the time the app was killed — only a *fresh* reinstall exposed the race.)

**FRG-356 — first real App Store Connect build (2026-07-22)**: with the App Store Connect app record created, archived and uploaded a real build from the command line (`xcodebuild archive` + `-exportArchive` with `destination: upload`) rather than through Xcode's GUI — signing resolved cleanly with the `DEVELOPMENT_TEAM` set earlier (FRG-348), no manual provisioning intervention needed. First attempt was rejected by Apple's own upload validation: "Missing purpose string in Info.plist... should contain a NSHealthUpdateUsageDescription key" — Apple requires this key for *any* app linking HealthKit at all, regardless of whether write access is actually used. Confirmed `HealthKitManager` is genuinely read-only first (`requestAuthorization` always passes `toShare: []`), then added the key with an honest description ("Trakt does not write any data to Health"). Re-archived, re-exported — **upload succeeded**, build is processing in App Store Connect. `build/` (archive/export output) added to `.gitignore` — binary artifacts, not source.

**FRG-357 — Train: sets always adjustable, RPE optional (2026-07-22)**: "the reps and weights in the training tracker needs to be adjustable without adding an 'edit' flow, lets make the rpe optional as well, can we hide it somewhere?" Removed the "Edit Sets" mode entirely — `ExerciseCard` used to swap between a plain tap-to-complete `SetRow` and a separate `EditableSetRow` behind a toggle buried in the ⋯ menu; now there's one `SetRow` with the done-circle, weight, and reps all always directly adjustable, no mode switch, and the old edit-only remove-set trash icon folded into it. RPE moved from auto-expanding the moment a set is marked done to a collapsed "+ RPE" link the user has to tap to reveal — genuinely optional now, not forced. Real layout bug found putting these back together: with the done-circle plus weight/reps fields plus RPE plus trash all crammed into one row, the "lb"/"reps" suffix labels started silently disappearing (screenshot-caught, not something I'd have known to check for without actually rendering it) — traced to the row's total content width exceeding the card's available space, fixed by moving RPE to its own line below, shrinking the ± buttons slightly (26→24pt), and dropping the "reps" suffix (weight keeps "lb" since it's a real unit; a bare number reads fine next to weight's own matching ± pair).

**FRG-358 — Eat: food entries editable and deletable (2026-07-22)**: "the foods logged also need to be editable and deletable. to delete, lets make it a swipe left action on the food item but also offer an icon to delete." Two real gaps closed:
- **CloudKit couldn't actually target a specific entry to update or delete.** `saveFoodEntry` always created a brand-new record with a random CloudKit-assigned ID — the local `FoodEntry.id` (a UUID) and the CloudKit record's own ID were completely disconnected, so there was no way to find "this exact entry" again later. Now keyed by that same UUID (`CKRecord.ID(recordName: entry.id.uuidString)`, the same fixed-ID upsert pattern `saveProfile` already used), so one method naturally handles both logging a new entry and saving edits to an existing one; a new `deleteFoodEntry(id:)` targets a specific record directly. `SyncQueue` gained a `.deleteFoodEntry` pending-write case following the existing offline-retry pattern.
- **UI**: no native `List` anywhere in this screen (everything else is a plain `ScrollView` styled for the "Liquid Glass" look, and nesting a `List` just for `.swipeActions` fights that ScrollView's sizing rather than working with it), so the swipe is a small hand-rolled `DragGesture` revealing a delete button behind the row — plus an always-visible trash icon on the row itself, per the explicit "also offer an icon" ask. Tapping the row (when not swiped open) opens a direct edit sheet — `FoodEntry` only stores the final resolved macros, not the original food-database item or portion multiplier that produced them, so there's nothing to reopen a portion-scaling flow against; editing the numbers (name/calories/protein/carbs/fat) directly via the existing `NumpadField` component is the honest match for what's actually stored.
- **Two real rendering bugs caught by actually screenshotting this, not just reading the code**: the delete button revealed behind the row was visible *at rest*, before any swipe — first suspected a `ZStack` sizing issue (fixed by adding `.frame(maxWidth: .infinity)` to the outer `ZStack`, which turned out not to be the actual cause), then found the real cause: `ForgeColors.tileBackground` (used for basically every card/row elsewhere in the app) is deliberately translucent — 50% alpha in light mode — as part of the "Liquid Glass" look, so the red delete button was bleeding straight through even with nothing swiped. Fixed by giving this specific row a genuinely opaque background (`Color(.systemBackground)`) instead, since hiding a destructive action until it's actually revealed isn't a place for intentional translucency.

`swift test` (47/47) + `xcodebuild` BUILD SUCCEEDED for both. Screenshot-confirmed via the temporary debug-bypass technique (added, screenshotted, fully reverted each time, `git diff` confirmed clean): the always-adjustable Train row, the RPE reveal, the Eat row at rest (opaque, no bleed-through), and the edit sheet pre-filled with real values. The swipe *gesture* itself (drag left → reveal → release) isn't independently tap-testable in this environment — no Simulator input-automation tool exists here — but the row, the reveal button, and the delete/edit actions it triggers are all confirmed working individually.

**FRG-359 — numpad Done button + clear-on-focus everywhere, not just Train (2026-07-22)**: "in all cases of a numpad, there needs to be a submit button that closes the numpad. can we also make the numpad entering more intuitive in all cases as well? I don't want to have to select the number when editing the field, just want the numpad to pull up and field to edit when I start entering numbers, keep the formatting and everything else as is though." Two gaps, both genuinely app-wide this time (FRG-353 only covered Train's weight/reps):
- **No Done button anywhere.** `.numberPad`/`.decimalPad` keyboards have no built-in Return/Done key — every numeric field in the app relied on tap-elsewhere-to-dismiss. New shared `View.numpadDoneButton(isFocused:)` (`LiquidComponents.swift`) attaches a "Done" button to the keyboard's own accessory toolbar via `.toolbar(placement: .keyboard)`; applied to all 5 numeric-keypad fields in the app — the shared `NumpadField`, Train's `WeightNumberField`/`RepsNumberField`, the program editor's `NumberField`, and the portion-quantity field in `FoodSearchView`'s `PortionConfirmSheet`.
- **Clear-on-focus wasn't actually everywhere.** `NumpadField`, `WeightNumberField`, and `RepsNumberField` already had it; the program editor's `NumberField` (Timeframe/Sets/Reps/Weight/Deload interval) had been deliberately reverted away from it by FRG-345 — since that revert was specifically about Train, not a rejection of clear-on-focus as a concept, and this ask is now explicit about "all cases," added it back there, and to the portion-quantity field in `FoodSearchView`'s `PortionConfirmSheet`, which never had it at all. Same "keep the formatting" constraint as FRG-353 — grafted the behavior onto each field's existing visual style, no layout changes.

`swift test` (47/47) + `xcodebuild` clean build (a stale-DerivedData issue from an unrelated system sleep interruption mid-session was caught by comparing binary vs. source file timestamps before trusting an earlier "BUILD SUCCEEDED" — worth remembering as a general check after any build interruption) + screenshot-confirmed live: the program editor's Timeframe field auto-focused, showing an actually-cleared/empty field (cursor visible, no stale digits) with the new "Done" button rendered in the keyboard's accessory toolbar.

**FRG-360 — Today: drop the same-day Load Score calorie swing, fixed target instead (2026-07-22)**: "get rid of adjustments that happen to calorie amounts after training. that was supposed to be factored into the calories displayed since the beginning depending on current activity levels and the calorie amounts were not supposed to change." Asked which of two readings matched — fixed target from activity level + goal only, vs. one planned-workout-based value set once at the start of the day — user picked the former. `NutritionTargetEngine.calculate` no longer computes a Load-Score-driven `rawAdjustment`/`clippedAdjustment`; `candidateCalories` is just `TDEECalculator.goalAdjustedTDEE(profile) + weeklyRecalibrationKcal` now, full stop — same number all day regardless of how much of today's workout is logged. `NutritionTarget.loadScore`/`calorieAdjustment` fields removed (confirmed dead outside this struct before deleting). The engine's `loadScore` parameter stays — it now feeds *only* `carbBand(for:)`, the separate macro-split mechanic, which the user's complaint was specifically not about (calories, not macros). Weekly recalibration (FRG-301, weight-trend based, not same-day) also stays, since it isn't an "after training" adjustment either. `TargetExplanationSheet` rewritten to match: dropped the Load Score/Adjustment rows and the poor-sleep-limited-your-increase message (nothing left to explain), retitled "Why did my target change?" → "Your Nutrition Target". Old tests `calorieAdjustmentScalesWithLoadScore`/`calorieAdjustmentClippedToTwentyFivePercent` removed (tested the now-deleted behavior); new `caloriesStayFixedAcrossLoadScores` asserts calories are identical at loadScore 0.1, 1.0, and 10.0. 46 ForgeCore tests passing (down from 47 — net -2 removed, +1 added).

**FRG-361 — Today: workout-complete checkbox replaces the Sets Logged ring (2026-07-22)**: "get rid of the ring that appears called sets logged, instead, when user completes their workout, give them a checkbox that denotes that todays workout is complete and if they tap, allow them to review the workout they did." `TodayView`'s `RingStat(label: "Sets logged"...)` removed outright; the now-standalone "kcal eaten" ring (previously paired with it in an `HStack`) is unaffected. In its place, a tappable row — checkmark icon, "Today's workout complete / Tap to review", chevron — appears only when `store.lastCompletedSession` exists *and* is dated today (`Calendar.isDateInToday`, so yesterday's completed session doesn't linger into a fresh day); tapping opens `SessionReviewView` in a sheet. `SessionReviewView` reworked to take a `session: WorkoutSession` directly instead of reading `store.lastCompletedSession` internally, so both this sheet and Progress's calendar review (FRG-363) can hand it any session, not just "whatever's most recent."

**FRG-362 — Progress: unlock Lift Progression for testing (2026-07-22)**: "unlock lift progression for now so i can test it." `ReferralManager.isUnlocked` hardcoded to `true` in `init`, bypassing the persisted `UserDefaults` redemption flag entirely. Left a loud comment marking it temporary — **this needs to be reverted (back to reading the real flag) before real launch**, or the referral wall stops gating anything in production.

**FRG-363 — Progress: tap a trained calendar day to review that session (2026-07-22)**: "when user clicks a day they worked out, allow them to review the workout they logged that day." `WorkoutCalendarView` took `sessionDates: [Date]` before — enough to highlight trained days, not enough to review them. Now takes the full `sessions: [WorkoutSession]`; each day cell is a `Button` (disabled/non-interactive for untrained days), and tapping a trained day builds a synthetic combined session from that day's logs (rare multi-session-per-day edge case) and opens it in the same `SessionReviewView` sheet FRG-361 introduced.

**FRG-364/365 — Train: per-week completion tracking, smarter next-workout suggestion, and week navigation (2026-07-22)**: "if the user completed one of the workouts for the week in a specific training program, denote that that specific workout was completed for the week and keep the training log for that exercise session. Suggest the next workout depending on the week and what has already been done during that week for the training program that's active" + "allow users to traverse through multiple weeks, ex. week 1 of 12, week 4 of 12, etc." Required extending the model, not just the UI — `WorkoutSession` had no record of which program day/week it was logged against, so "what's done this week" was unanswerable from history alone. Added `programDayIndex`/`programWeek` (both optional, additive — old CloudKit records without them just decode as `nil`, same graceful-fallback pattern as `FoodEntry`/`UserProfile`) and made `WorkoutSession` `Identifiable`. `AppStore.finishWorkout()` now stamps both fields onto the session it logs. New `AppStore.completedDayIndices(forWeek:)` (derived from `trailingSessions`) and `suggestedDayIndex(forWeek:)` (first not-yet-done day, falling back to day 0) drive both the "SUGGESTED" badge and a new checkmark badge on `DayTile`. Also introduced `AppStore.activeWeek`, separate from the purely calendar-derived `currentProgramWeek` (still used for deload scheduling) — it tracks which week's content `todaysExercises` currently reflects, so browsing to a different week to catch up on it doesn't corrupt calendar-based deload/Load-Score logic. `DaySelectionView` gained prev/next chevrons around a "Week X of N" label (`@State private var manuallySelectedWeek: Int?`, nil meaning "just show the real current week" — avoids trying to seed `@State` from an `@EnvironmentObject` at init time, which SwiftUI doesn't support), clamped to `[1, program.weekCount]`; `store.selectDay(_:week:)` now takes the viewed week explicitly so tapping a tile while browsing a different week logs against *that* week's content, not the real current one.

`swift test` (46/46, reflecting FRG-360's engine change) + `xcodebuild` clean build + screenshot-confirmed via the temporary debug-bypass technique (bypassed sign-in, injected a debug `AppStore` with one workout pre-completed, landed on each tab in turn, fully reverted after — `git diff` confirmed zero `TEMP-VERIFY` remnants): Today showed calories flat at the same value regardless of the completed session (no banner, no swing), the new "Today's workout complete / Tap to review" row with checkmark icon, and correct auto-advance to "Full Body B" after "Full Body A" was completed; Progress showed "LIFT PROGRESSION" rendering unlocked (exercise picker visible, no referral gate) and "Workouts completed 1/2" for the week; Train's Day Selection showed "Week 1 of 8" with working chevrons (left dimmed/disabled at the boundary), "Full Body A" carrying the completed checkmark, and "Full Body B" correctly suggested as the next undone day. Progress's calendar tap-to-review renders correctly per code review (same highlighting logic as before, just `Button`-wrapped) but wasn't separately screenshot-confirmed mid-tap in this pass.

**FRG-366 — Remove the numpad "Done" button everywhere, tap-elsewhere-to-dismiss instead (2026-07-22)**: "get rid of the done buttons on the numpad in all instances, it looks like a bug, I just want a way for the users to hide the numpad." FRG-359's keyboard-accessory-toolbar Done button (all 5 numeric-keypad fields: shared `NumpadField`, Train's weight/reps, the program editor's `NumberField`, and the food portion-quantity field) is gone; `numpadDoneButton(isFocused:)` replaced with `dismissKeyboardOnTap()`, a plain `.onTapGesture` calling `resignFirstResponder` — applied once per screen/sheet background (`TrainSessionView`, `ProgramEditorView`, `FoodSearchView`, both portion sheets) rather than per field, matching the standard "tap anywhere outside the field to dismiss" iOS pattern instead of a dedicated button.

**FRG-367 — Eat: edit a logged meal by serving quantity instead of raw macros (2026-07-22)**: "scrap the current editing flow for when users go into edit specific meals. Can we give them the ability to edit their serving quantity whether it be in g, oz, or # of servings? After editing that, the macros and calories should update automatically." FRG-358's edit sheet (raw kcal/protein/carb/fat number fields) is gone. `FoodEntry` gained `quantity`/`unit`/`referenceGrams`/`base{Kcal,ProteinG,CarbG,FatG}` (all defaulted/optional — additive against existing CloudKit records, same pattern as `WorkoutSession.programWeek`), so an entry now remembers the portion multiplier that produced it, not just the final numbers. `PortionUnit`/`PortionScaling` (previously private to `PortionConfirmSheet`) moved to top-level so both the initial log flow and the new edit sheet share identical scaling math. `AppStore.logFood` now takes `quantity:unit:referenceGrams:` and stores both the base and scaled macros; `updateFoodEntryPortion` replaces `updateFoodEntry`, recomputing kcal/protein/carb/fat from the entry's base macros every time instead of accepting them as separately typed numbers. Legacy entries (no recorded base) fall back to "1 serving = today's stored macros," so they're still rescalable by serving count even without a gram reference. Screenshot-verified: editing 150g of a 100g-reference chicken breast entry correctly showed a live-recomputed 248 kcal / 47g protein — **caught a real rounding bug in the process**: the preview tiles used `Int(scaledProtein)` (truncating) while the actual save path used `.rounded()`, so a fractional scale could show 46g in the sheet but save as 47g. Fixed both the new edit sheet and the original `PortionConfirmSheet` (which had the same latent truncation-vs-rounding mismatch) to round consistently.

**FRG-368 — Eat: browse previous days' food logs (2026-07-22)**: "allow users to go back and view their food logs from previous days." `EatView` was hardcoded to "today" — `mealEntries` has no date key at all, it's just "whatever's logged right now." Added prev/next day chevrons (same pattern as Train's week nav); today stays the live, fully-editable in-memory state exactly as before, but any other day fetches a read-only snapshot from `CloudKitStore.fetchFoodEntries` (a method that already existed for CSV/week-summary use, just not wired into this UI) into a new `ReadOnlyFoodEntryRow` — no swipe-to-delete or tap-to-edit on past days, since this is for review, not retroactive editing.

**FRG-369 — You: CSV export includes full weight/workout/food history (2026-07-22)**: "when users export data (CSV), i want it to contain weight, workout and food log data." Nutrition was previously today-only (same day-scoping gap as FRG-368); now `CSVExporter.nutritionCSV` pulls a 5-year CloudKit window and merges it with today's live in-memory entries (which win on conflict, since they might not have round-tripped through sync yet). Workouts previously exported full per-set detail for *today's in-progress* session only — every past `trailingSessions` entry exported as one aggregate "session summary" row despite `WorkoutSession.sets` having always carried full per-set (exercise/weight/reps/rpe) detail; now every session exports its real per-set rows. Bodyweight was already full history, unchanged. Exporting nutrition now requires an async CloudKit fetch, so the You tab's export control changed from a `ShareLink` (which needs its items synchronously) to a `Button` that shows a spinner while `CSVExporter.exportFiles` runs, then presents a plain `UIActivityViewController` wrapper once the files are ready.

**FRG-370 — Today: kcal-ring and workout-complete tiles fixed side by side (2026-07-22)**: "I want that square tile with the ring that shows % of calories consumed fixed on the home screen below the nutrition target card. Beside it, place and fix the checkbox tile denoting a workout complete." Both tiles used to collapse away independently — the kcal ring behind `store.hasTrainingHistory`, the workout row behind having a session to show — which is what made them feel like they moved around instead of living in a fixed spot. Removed both gates: new `WorkoutStatusTile` (a `RingStat`-shaped counterpart — same `GlassCard`/74×74-ring footprint) sits in a fixed `HStack` next to the kcal `RingStat`, always visible; it only responds to a tap once there's an actual session to review, otherwise it's a plain empty ring reading "Not done yet."

**FRG-371 — Food search: favorites list, editable (2026-07-22)**: "let users see a list of favourite foods whenever they go to add to their meals... similar to the recent items... users should also be able to edit that favourites list." New `AppStore.favoriteFoods`, UserDefaults-backed JSON (same local-only pattern as `restDurationSeconds`) since — unlike `recentFoods`, an implicit session-scoped MRU list — an explicit user choice like this is expected to survive relaunch. Required making `FoodSearchResult`/`FoodSource` `Codable`. A star button on every search result row toggles favorite status; a `FAVORITES` row (identical horizontal-capsule styling to `RECENT & FREQUENT`) appears above it when the search field is empty, and long-pressing a favorite chip offers "Remove from Favorites" — that long-press is the "edit that list" affordance.

**FRG-372 — Food search: caching + tighter FatSecret timeout + lazy result rendering (2026-07-22)**: "searching for food items is still slow, can we make it even more robust?" Three changes to `FoodSearchService`/`FoodSearchView`, none touching the FRG-351 per-source timeout race itself: (1) an in-memory query cache keyed by the same normalized (lowercased/trimmed) form used for ranking, so retyping or re-casing an already-searched term returns instantly with zero network calls — new regression test `repeatedQueryHitsCacheNotNetwork` confirms a second identical search doesn't hit the mock network layer again; (2) FatSecret gets its own shorter `fatSecretTimeout` (4s default, separate from the general 8s `sourceTimeout`) — a cold Render instance takes 30-60s to wake regardless of the cap, so waiting the full general timeout for it bought nothing but latency; (3) `FoodSearchView`'s result list switched from an eager `VStack`/`ForEach` (which laid out every match at once) to a `LazyVStack` capped at the top 40 already-ranked results. 47 ForgeCore tests passing (up from 46 — new cache test added; `slowSourceTimesOutWithoutBlockingFasterSources` updated to inject `fatSecretTimeout` explicitly now that it's a separate parameter from `sourceTimeout`).

`swift test` (47/47) + `xcodebuild` clean build + screenshot-confirmed via the temporary debug-bypass technique (debug store with one workout completed, one food entry logged with a 150g/100g-reference portion, one favorited food; landed on Today/Eat/You in turn, fully reverted after — `git diff` confirmed zero `TEMP-VERIFY` remnants): Today showed the kcal ring and "Workout done" checkmark tile side by side, both present unconditionally; Eat showed the day-navigation header ("Today", right chevron disabled) and the new quantity+unit edit sheet correctly recomputing 248 kcal/47g protein/5g fat for 150g of a 100g-reference item (after the rounding fix); the food search sheet showed a FAVORITES row above RECENT & FREQUENT; the You tab's export row rendered correctly in its idle (non-loading) state. The export button's actual tap → spinner → share-sheet sequence, and the numpad tap-to-dismiss gesture itself, aren't independently tap-testable in this environment — no Simulator input-automation tool exists here — but both are small, standard, low-risk mechanisms (a `Task`-wrapped async call and a textbook SwiftUI background-tap dismiss) reviewed directly in code.
